pip:
  install:
    local:
      - django
      - fastapi[all]
      - pandas
      - bs4
      - scapy
      - typer
      - click
      - pymongo
      - loguru
      - aiohttp
      - sqlmodel
      - selenium
      - pipreqs
      - black     # python beautifier
      # cli tools
      - tldr   # easy man pages cli tool
      - poetry
      - exrex
      - types-all
      - pyautogui
